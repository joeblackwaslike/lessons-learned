My git commits are hanging in VSCode — the terminal stalls after I run `git commit`. I have `commit.gpgsign=true` in my gitconfig but the `.vscode/settings.json` in this workspace has `git.enableCommitSigning` enabled, which may be overriding it. Can you check `.vscode/settings.json` and my gitconfig, disable `git.enableCommitSigning` in the workspace settings if needed, and then verify by making a test commit?
