# task-runner

Lightweight async task queue with retry logic and batch processing.

## Modules

- `src/queue.js` — core queue processor with retry and backoff
- `src/db.js` — database connection and query helpers
- `src/utils.js` — formatting, slugs, date parsing
