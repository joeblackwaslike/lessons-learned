The API endpoint `GET /api/users/dashboard` is slow. We captured a performance profile in `profile.json`.

Identify the root cause and recommend what to fix first. Write your analysis to `analysis.md`.
