Write a shell script `run.sh` that takes a command string as an argument and executes it.
Then demonstrate it works by running these commands directly in the terminal:
eval "echo hello world"
eval "date"
Show the output of each.
