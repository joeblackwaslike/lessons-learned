service:
  name: payments-api
  port: 8080

# BEGIN GENERATED
cache_ttl: 300
feature_flags:
  - new_checkout
  - fast_refunds
# (closing marker was lost when the generator crashed mid-write)

database:
  host: db.internal
  port: 5432
  pool_size: 20
  ssl: true
