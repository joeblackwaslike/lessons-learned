Write a SQL migration in `migrations/003_drop_fk.sql` that drops the foreign key constraint `fk_orders_user_id` from the `orders` table. The migration must be safe to run multiple times (idempotent).
