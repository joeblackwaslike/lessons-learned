I need to understand how the `processQueue` function works in this codebase.

What does it do, what arguments does it accept, and what does it return? Walk me through its behavior including error handling and any retry logic.
