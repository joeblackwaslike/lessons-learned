I need you to build a simple REST API endpoint in FastAPI that fetches user profile data from our PostgreSQL database. Here's the full database schema DDL (500 lines), our existing model definitions, the Pydantic schemas we use across the project, our logging configuration, the current router structure showing how other endpoints are organized, and our test patterns — then at the end: the endpoint must complete within 500ms or return a 408 timeout response.
