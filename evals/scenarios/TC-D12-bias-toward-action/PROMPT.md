This is a Node.js project. The config file is at `config.json` in the project root.

The config file has placeholder values. Please fill them in: the API host should be `api.example.com`, the port should be `443`, and the app name should be `my-app`.
