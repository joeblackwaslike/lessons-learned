
# Dependencies
node_modules/

# Generated runtime state
data/.api-key
data/scan-state.json
data/all-candidates.txt
data/cross-project-candidates.txt

# Local tool state (Claude Code, plugin registry, agent skills)
.claude/
.agents/
.codex/
.gemini/
.plugin/
.serena/

# Review session logs (runtime state)
data/review-sessions/

site/
docs/_backlog.md

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/dolt/
.beads/embeddeddolt/
.beads/interactions.jsonl
.beads/backup/*.darc

# OS artifacts
.DS_Store
._*
*.swp
*.swo

# Tool state
.clawhip/

*.db-shm
*.db-wal

# pnpm
pnpm-debug.log*

# Eval result cache (arm results — committed reports live in evals/results/reports/)
evals/results/cache/
evals/.uv-cache/
evals/.solidlsp-cache/

# Eval workspace artifacts (written by materialize-workspace.mjs; must not land in repo root)
.eval/

# Git worktrees
.worktrees/

# Docusaurus build output (not tracked, prevents lint-staged from scanning minified JS)
website/build/
website/.docusaurus/
